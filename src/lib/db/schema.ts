// Database schema definitions (for future PostgreSQL integration)
// This file will contain table schemas when migrating from Notion to PostgreSQL

export const TABLES = {
  USERS: 'users',
  CHEMICALS: 'chemicals',
  LOCATIONS: 'locations',
  TRANSACTIONS: 'transactions',
} as const;

// TODO: Add actual schema definitions when setting up database
